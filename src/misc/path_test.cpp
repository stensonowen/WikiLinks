#include<iostream>
#include "BFS.h"
using namespace std;
int main(){
    //shared_ptr<Path> p(new Path());
    //shared_ptr<Path> q(p->generate_child(1));
    //cout << q->get_destination() << endl;
    //shared_ptr<Path> r;
    //r = p->generate_child(2);
    //cout << r->get_destination() << endl;
    //shared_ptr<Path> s((p->generate_child(2)));
    //cout << s->get_destination() << endl;
    return 0;
}
